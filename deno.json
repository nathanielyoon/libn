{
  "license": "GPL-3.0-or-later",
  "workspace": [
    "./aead",
    "./base",
    "./csv",
    "./ecc",
    "./fp",
    "./hash",
    "./json",
    "./query",
    "./signal",
    "./text"
  ],
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.15",
    "@std/expect": "jsr:@std/expect@^1.0.17",
    "@std/testing": "jsr:@std/testing@^1.0.16",
    "fast-check": "npm:fast-check@^4.3.0"
  },
  "compilerOptions": {
    "exactOptionalPropertyTypes": true,
    "verbatimModuleSyntax": true
  },
  "permissions": {
    "esbuild": {
      "env": true,
      "run": true
    },
    "write-vectors": {
      "net": [
        "www.rfc-editor.org",
        "raw.githubusercontent.com",
        "en.wikipedia.org",
        "earthquake.usgs.gov",
        "www.unicode.org",
        "crockford.com",
        "www.ietf.org"
      ],
      "write": true
    }
  },
  "lint": {
    "rules": {
      "include": [
        "camelcase",
        "eqeqeq",
        "no-console",
        "no-external-import",
        "no-inferrable-types",
        "no-self-compare",
        "no-sparse-arrays",
        "no-useless-rename",
        "prefer-ascii"
      ],
      "exclude": [
        "ban-types",
        "no-cond-assign",
        "no-control-regex",
        "no-explicit-any",
        "no-invalid-regexp"
      ],
      "tags": ["recommended", "jsr"]
    }
  },
  "tasks": {
    "bundle": {
      "description": "Bundle files (with flags and file names passed to `deno task`).",
      "command": "deno run --permission-set=esbuild npm:esbuild --bundle --format=esm --external:@libn"
    },
    "write": {
      "description": "Runs a test-vector-writing script (with file name and arguments passed to `deno task`).",
      "command": "deno run --permission-set=write-vectors"
    },
    "vectors": {
      "description": "Runs all test-vector-writing scripts.",
      "command": "rm -f **/vectors.json; echo \"{}\" >./aead/vectors.json && deno task --cwd=./aead write test.ts; echo \"{}\" >./base/vectors.json && deno task --cwd=./base write test.ts; echo \"{}\" >./csv/vectors.json && deno task --cwd=./csv write test.ts; echo \"{}\" >./ecc/vectors.json && deno task --cwd=./ecc write test.ts; echo \"{}\" >./hash/vectors.json && deno task --cwd=./hash write test.ts; echo \"{}\" >./text/vectors.json && deno task --cwd=./text write test.ts"
    },
    "check": {
      "description": "Type-check files.",
      "command": "cd \"$INIT_CWD\" && deno check --doc"
    },
    "lint": {
      "description": "Lint code and documentation.",
      "command": "cd \"$INIT_CWD\" && deno lint --compact && deno doc --lint **/*.ts"
    },
    "test": {
      "description": "Run tests.",
      "command": "cd \"$INIT_CWD\" && deno test -P --doc --no-check --parallel --trace-leaks"
    },
    "record": {
      "description": "Collect coverage data.",
      "command": "cd \"$INIT_CWD\" && deno task --cwd=. test --clean --coverage=/tmp/deno-coverage --coverage-raw-data-only"
    },
    "report": {
      "description": "Display coverage data.",
      "command": "cd \"$INIT_CWD\" && deno coverage --detailed /tmp/deno-coverage --include=\"^file://$(pwd)\""
    },
    "cover": {
      "description": "Collect and display coverage data.",
      "dependencies": ["record", "report"]
    },
    "pure": {
      "description": "Checks whether the current directory's module has side-effects, which would be included in the generated output.",
      "command": "cd \"$INIT_CWD\" && echo \"import \\\"./mod.ts\\\";\" >bundle.ts && deno task --cwd=. bundle ./bundle.ts; rm -f ./bundle.ts;"
    },
    "all": {
      "description": "Run all check tasks.",
      "dependencies": ["check", "lint"],
      "command": "cd \"$INIT_CWD\" || exit; deno task --cwd=. test && deno task --cwd=. cover; deno task --cwd=. pure; deno publish --dry-run --allow-dirty"
    }
  }
}
